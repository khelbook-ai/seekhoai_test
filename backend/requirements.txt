fastapi==0.115.6
uvicorn[standard]==0.34.0
psycopg[binary,pool]==3.2.3
pydantic==2.10.4
pydantic-settings==2.7.1
PyYAML==6.0.2
python-dotenv==1.0.1
python-multipart==0.0.20            # feedback image uploads (spec 06 §2)
# Graph orchestration (spec 02). Real build + session graphs with Postgres checkpointer.
langgraph==0.2.60
langgraph-checkpoint-postgres==2.0.9
# MCP tool server (spec 05).
mcp==1.2.0
# LLM access — all providers via OpenRouter's OpenAI-compatible API (spec 02 §4).
openai==1.59.6
tenacity==9.0.0                     # retry/backoff on model + tool calls
# Live-web scouting & extractors (spec 05 §2).
httpx==0.28.1
beautifulsoup4==4.12.3
trafilatura==2.0.0
pypdf==5.1.0
pypdfium2==4.30.0                   # render PDF pages -> images for vision figure extraction
arxiv==2.1.3
python-pptx==1.0.2
python-docx==1.1.2
ddgs==9.0.0                         # keyless web search
Pillow==11.1.0                      # figure/image handling
# Observability (spec 06 §3) — LangSmith primary, Phoenix pluggable.
langsmith==0.2.10
# tests
pytest==8.3.4
