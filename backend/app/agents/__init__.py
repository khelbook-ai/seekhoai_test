"""Agent implementations (spec 03). Each module is a pure function over typed I/O,
resolving its model from the registry and calling app.llm. No hardcoded model IDs,
no inline prompt strings (prompts live in prompts/<agent>.md)."""
