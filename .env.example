# Copy to .env and fill in. docker compose reads this automatically.

# Required: OpenRouter key for all model + web-search calls (spec 02 §4).
OPENROUTER_API_KEY=

# Optional: LangSmith tracing (observability is off by default, spec 06 §3).
# LANGSMITH_API_KEY=
